np_density_tab <- tabItem(tabName="np_density",
  h1("Nucleoprotein density"),
  fluidRow(
    box(
      title="Input",
      width=12,
      textInput(
        inputId="",
        label=""
      )
    )
  )
)
